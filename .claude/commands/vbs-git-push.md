---
description: Push committed changes for a VBS repo checkout
argument-hint: [repo]
allowed-tools: mcp__vbcs__git_push
---
Repo argument: $ARGUMENTS

Call the git_push MCP tool (mcp__vbcs__git_push) with that repo (omit if none given). Confirm success and what was pushed, or report the error clearly if it failed.
