---
description: Pull latest changes for a VBS repo checkout
argument-hint: [repo]
allowed-tools: mcp__vbcs__git_pull
---
Repo argument: $ARGUMENTS

Call the git_pull MCP tool (mcp__vbcs__git_pull) with that repo (omit if none given). Report what changed, or that it was already up to date.
