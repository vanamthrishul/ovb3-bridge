---
description: List applications on a standalone VBCS instance
allowed-tools: mcp__vbcs__vbcs_list_applications
---
Call the vbcs_list_applications MCP tool (mcp__vbcs__vbcs_list_applications) with no filter. Show the result as a plain bullet list. If it fails due to missing config, mention that VBCS_BASE_URL/VBCS_IDCS_USERNAME/VBCS_IDCS_PASSWORD need to be set (run /link) and that a session restart is required after editing .env.
