---
description: List VBCS web apps present in a repo checkout
argument-hint: [repo]
allowed-tools: mcp__vbcs__list_apps
---
Repo argument: $ARGUMENTS

Call the list_apps MCP tool (mcp__vbcs__list_apps) with that repo (omit the repo argument entirely if none was given, to use the VBCS_REPO_PATH default). Show the result as a plain bullet list.
